CREATE FUNCTION [dbo].[xfn_get_randomize_password]
(@new_id UNIQUEIDENTIFIER NULL)
RETURNS NVARCHAR (12)
WITH ENCRYPTION
AS
BEGIN
--The script body was encrypted and cannot be reproduced here.
    RETURN NULL
END

